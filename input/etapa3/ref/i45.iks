/* the insertion sort by ta ga */
int : insertionSort (int: vetor, int: tamanho)
int : i;
int : aux;
{
	i = 1;
	while (i < tamanho) do
	{
		while(( i != 0) && (vetor[i] < vetor[i-1])) do
		{
			aux = vetor[i];
			vetor[i] = vetor [i-1];
			vetor[i-1] = aux;
			i = i - 1;
		};
		i = i+1;
	};

}
